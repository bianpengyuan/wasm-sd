syntax = "proto3";

package config;

message StackdriverModule {
  bool enable_logging = 1;
  bool enable_monitoring = 2;

  string project_id = 3;
  string monitored_resource_type = 4;
  map<string, string> monitored_resource_labels = 5;

  int64 flush_interval_sec = 6;
}
